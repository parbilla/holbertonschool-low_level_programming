#include <stdio.h>

/**
 * main - prints its name
 * @argc: count
 * @argv: string
 *
 * Return: result
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
