#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	/* Get the alphabet */
	int c;

	for (c = 0; c <= 9; c++)
	{
		int putchar(int c);
	}

	putchar('\n');
	return (0);
}
