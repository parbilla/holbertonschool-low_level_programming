#include "holberton.h"

/**
 * is_prime_number - looks for a prime number
 * @n: number
 *
 * Return: result
 */

int is_prime_number(int n)
{
	if (n == 1)
	{
		res = 1;
	}
	else if (n > 3)
	{
		if (n / div == div)
		{
			res = div;
		}
		else
		{
			div--;
			_sqrt_recursion
		}
	}
	else
	{
		res = -1;
	}
	return (res);
}
